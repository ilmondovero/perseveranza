// slugify(input) -> string
// Specifica:
//   - input non stringa (numero, null, undefined, oggetto...) -> lancia TypeError
//   - minuscole; spazi/underscore e ogni carattere non alfanumerico diventano '-'
//   - accenti/diacritici rimossi (es. "perché no?" -> "perche-no")
//   - sequenze di '-' collassate a uno; niente '-' in testa o in coda
//   - stringa vuota o di soli separatori -> ''
export function slugify(input) {
  // TODO: implementare secondo la specifica sopra
  throw new Error('non implementata');
}
